import 'package:ism_app/imports.dart';

class ErrorView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Error in fetching details"),
    );
  }
}
